# Helm Values Files

Custom values files for Helm charts deployed via Helmfile.

## Charts

- **longhorn**: Distributed block storage system
- **metallb**: Load balancer for bare-metal Kubernetes
- **traefik**: Modern HTTP reverse proxy and load balancer

Each subdirectory contains a `values.yaml` file with configuration overrides for the respective chart.